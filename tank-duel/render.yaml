services:
  - type: web
    name: tank-duel
    runtime: node
    plan: free
    rootDir: tank-duel
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20.11.0
